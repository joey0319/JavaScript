// let은 재할당 가능
let a = 1;
a = 2;

// const는 재할당 불가
const b = 1;
b = 2;

// 상수처럼 사용 할 때 쓴다.
const MAX_FRUITS = 5;

// 재할당이 필요없는 상수 변수 또는 변수로 사용한다.
const apple = {
    name: 'apple',
    color: 'red'
};
